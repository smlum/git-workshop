{
  "name": "git-workshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "reveal-md slides.md -w",
    "static": "reveal-md slides.md --css style.css --static docs --static-dirs=images && mv docs",
    "static-images": "reveal-md slides.md --static --static-dirs=images && mv docs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "reveal-md": "^5.4.0"
  }
}
